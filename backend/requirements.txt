# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi==0.111.0          # The web framework for building our API endpoints
uvicorn[standard]==0.29.0 # The ASGI server that actually runs FastAPI

# ── Settings & Environment ────────────────────────────────────────────────────
pydantic-settings==2.2.1  # Loads .env variables into typed Python classes
python-dotenv==1.0.1      # Reads the .env file from disk

# ── GitHub Integration ────────────────────────────────────────────────────────
PyGithub==2.3.0           # GitHub REST API client (fetch issues, open PRs)
gitpython==3.1.43         # Git operations in Python (clone, branch, commit, push)

# ── AI / LLM ─────────────────────────────────────────────────────────────────
groq==0.9.0               # Official Groq SDK — free, fast, runs Llama 3.3 70B

# ── RAG / Vector Database ────────────────────────────────────────────────────
chromadb==0.5.0           # Vector database to store and search code embeddings

# ── Docker Sandbox ────────────────────────────────────────────────────────────
docker==7.1.0             # Docker SDK to spin up containers from Python

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.0             # Test runner for the backend unit tests
httpx==0.27.0             # HTTP client used to test FastAPI endpoints
